# ShoppingCartPlaywright
1. import the repository in Webstorm IDE.Configure Playwright setup
2. Run npm install
3. npx playwright test ./tests/sanity/modifyCart.spec.ts --headed
(By default it uses chrome browser)
